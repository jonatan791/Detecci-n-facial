# Instalar con: pip install -r requirements.txt
#
# NOTA sobre dlib/face_recognition en Windows:
# face_recognition depende de dlib, que necesita compilarse con CMake
# y un compilador de C++. Si "pip install face_recognition" falla:
#   1) Instala CMake:  pip install cmake
#   2) Instala Visual Studio Build Tools (C++ build tools)
#   3) O, más simple, usa Anaconda y corre:
#        conda install -c conda-forge dlib
#        pip install face_recognition
#
opencv-python>=4.8
face_recognition>=1.3.0
numpy>=1.24
pandas>=2.0
openpyxl>=3.1
pyodbc>=5.0
